{
  "name": "yourhealthcareagent-site",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build-spec": "node scripts/build-spec.mjs",
    "dev": "npm run build-spec && wrangler dev",
    "deploy": "npm run build-spec && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
